// Leave empty for local development. After deploying the backend to Render,
// replace the value with its public URL, for example:
// window.PARKEASE_API_URL = 'https://parkease-api.onrender.com';
window.PARKEASE_API_URL = '';
